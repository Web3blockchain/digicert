/*
    walletAddress: string,




*/
const initState = {
    walletAddress: '',
};

const digiCertReducer = (state=initState, action) => {
    switch(action.type){
        default: 
            return state;
    }
}